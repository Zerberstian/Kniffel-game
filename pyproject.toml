[project]
name = "kniffel"
version = "0.1.0"
description = "Digitale Umsetzung des Würfelspiels Kniffel (Yahtzee)"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
